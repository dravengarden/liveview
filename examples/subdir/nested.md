# Nested File

This file lives in `examples/subdir/` to test the file tree sidebar handles nested directories correctly.

## Purpose

- Verify directory tree renders with expand/collapse
- Verify nested file paths resolve correctly
- Verify the file watcher picks up changes in subdirectories

## Relative Link Test

Back to parent: [README](../README.md)

## Small Code Block

```rust
fn nested() -> &'static str {
    "hello from subdir"
}
```
