{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults for the liveview native shell: core IPC, the opener plugin (settings deep-link) and native haptics. The lvsync plugin tokens were removed — the offline data layer currently runs as Swift controllers, not the Rust plugin (re-added in B2 when the Rust+SQLite plugin is registered).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "haptics:allow-impact-feedback",
    "haptics:allow-notification-feedback",
    "haptics:allow-selection-feedback",
    "haptics:allow-vibrate"
  ]
}
